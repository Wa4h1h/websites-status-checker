# websites-status-checker
A concurrent CLI utility written in Go that checks the HTTP status of multiple URLs in parallel using goroutines and channels. Supports configurable concurrency, timeouts, and input from files or command-line arguments. Displays per-URL results with response times and a summary of total, successful, failed, and non-200 responses.
